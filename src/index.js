import ReactDOM from 'react-dom/client';

 const myRoot = document.getElementById("root")

 const root = ReactDOM.createRoot(myRoot)
;
 const title =  <h1 style ={{color: 'green'}}> hello world</h1>

 root.render(title)